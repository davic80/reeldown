{
  "name": "reeldown",
  "version": "0.3.6",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:server": "PORT=3000 DATA_DIR=./.data node server/server.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "gen:icons": "npx @vite-pwa/assets-generator --config pwa-assets.config.js",
    "setup": "echo 'No server deps needed — uses node:sqlite built-in'"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^1.3.0"
  },
  "allowScripts": {
    "esbuild@0.25.12": true,
    "fsevents@2.3.3": true
  }
}
